import { Atom, Dispatcher } from '../types';
declare const dispatch: <T, D extends Dispatcher<T>>(atom: Atom<T>, dispatcher: (value: T) => D) => <R extends keyof D>(dispatchName: R, ...value: Parameters<D[R]>) => void;
export default dispatch;
