import { SuspenseResult } from '../types';
export declare const isSuspense: <T>(arg: T | SuspenseResult<T>) => arg is SuspenseResult<T>;
export declare const isFunction: (arg: any) => arg is Function;
